@extends('layouts.app')
@section('title', 'Phòng ban')
@section('content')
<x-page-header eyebrow="HR / Danh mục" title="Phòng ban" description="Quản lý đơn vị và phân bổ nhân sự."><a href="{{ route('hr.departments.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">+ Thêm phòng ban</a></x-page-header>
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><form method="GET" action="{{ route('hr.departments.index') }}" class="mb-6 flex flex-col gap-3 sm:flex-row"><label class="sr-only" for="department-search">Tìm phòng ban</label><input id="department-search" name="search" value="{{ request('search') }}" placeholder="Tìm theo mã hoặc tên..." class="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm sm:max-w-sm"><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700">Tìm kiếm</button><a href="{{ route('hr.departments.index') }}" class="rounded-lg px-4 py-2 text-sm font-semibold text-slate-500 hover:bg-slate-50">Đặt lại</a></form><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b border-slate-200 text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Mã</th><th class="px-3 py-3">Tên phòng ban</th><th class="px-3 py-3">Nhân viên</th><th class="px-3 py-3 text-right">Thao tác</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($departments as $department)<tr><td class="px-3 py-4 font-mono text-xs font-semibold text-indigo-700">{{ $department->code }}</td><td class="px-3 py-4 font-medium">{{ $department->name }}<p class="mt-1 text-xs font-normal text-slate-500">{{ $department->description }}</p></td><td class="px-3 py-4"><x-status-badge status="active" :label="$department->employees_count.' nhân viên'" /></td><td class="px-3 py-4"><div class="flex justify-end gap-3"><a href="{{ route('hr.departments.show', $department) }}" class="text-sm font-semibold text-slate-600">Xem</a><a href="{{ route('hr.departments.edit', $department) }}" class="text-sm font-semibold text-indigo-600">Sửa</a>@if ($department->employees_count === 0)<form method="POST" action="{{ route('hr.departments.destroy', $department) }}">@csrf @method('DELETE')<button onclick="return confirm('Xóa phòng ban này?')" class="text-sm font-semibold text-rose-600">Xóa</button></form>@endif</div></td></tr>@empty<tr><td colspan="4" class="px-3 py-4"><x-empty-state title="Chưa có phòng ban" description="Tạo phòng ban đầu tiên để bắt đầu quản lý." /></td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $departments->links() }}</div></div>
@endsection
