@extends('layouts.app')
@section('title', 'Nhân viên')
@section('content')
<x-page-header eyebrow="HR / Nhân sự" title="Danh sách nhân viên" description="Tra cứu và quản lý hồ sơ nhân sự."><a href="{{ route('hr.employees.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">+ Thêm nhân viên</a></x-page-header>
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><form method="GET" action="{{ route('hr.employees.index') }}" class="mb-6 grid gap-3 lg:grid-cols-[1fr_auto_auto_auto]" id="employee-filter"><input name="search" value="{{ request('search') }}" type="search" placeholder="Mã, tên hoặc email..." class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><select name="department_id" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Tất cả phòng ban</option>@foreach ($departments as $department)<option value="{{ $department->id }}" @selected((string) request('department_id') === (string) $department->id)>{{ $department->name }}</option>@endforeach</select><select name="employment_status" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Tất cả trạng thái</option><option value="active" @selected(request('employment_status') === 'active')>Đang làm việc</option><option value="inactive" @selected(request('employment_status') === 'inactive')>Đã nghỉ</option></select><div class="flex gap-2"><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700">Lọc</button><a href="{{ route('hr.employees.index') }}" class="rounded-lg px-3 py-2 text-sm font-semibold text-slate-500">Đặt lại</a></div></form><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b border-slate-200 text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Nhân viên</th><th class="px-3 py-3">Phòng ban</th><th class="px-3 py-3">Chức vụ</th><th class="px-3 py-3">Trạng thái</th><th class="px-3 py-3 text-right">Thao tác</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($employees as $employee)<tr><td class="px-3 py-4"><p class="font-medium text-slate-900">{{ $employee->user->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $employee->employee_code }} · {{ $employee->user->email }}</p></td><td class="px-3 py-4">{{ $employee->department->name }}</td><td class="px-3 py-4">{{ $employee->position ?: '—' }}</td><td class="px-3 py-4"><x-status-badge :status="$employee->employment_status" :label="$employee->employment_status === 'active' ? 'Đang làm việc' : 'Đã nghỉ'" /></td><td class="px-3 py-4"><div class="flex justify-end gap-3"><a href="{{ route('hr.employees.show', $employee) }}" class="text-sm font-semibold text-slate-600">Xem</a><a href="{{ route('hr.employees.edit', $employee) }}" class="text-sm font-semibold text-indigo-600">Sửa</a></div></td></tr>@empty<tr><td colspan="5" class="px-3 py-4"><x-empty-state title="Chưa có nhân viên" description="Tạo hồ sơ nhân viên đầu tiên để bắt đầu." /></td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $employees->links() }}</div></div>
@endsection
