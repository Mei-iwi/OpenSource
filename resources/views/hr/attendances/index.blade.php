@extends('layouts.app')
@section('title', 'Chấm công')
@section('content')
<x-page-header eyebrow="HR / Vận hành" title="Chấm công" description="Theo dõi và cập nhật chấm công nhân viên."><a href="{{ route('hr.attendances.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">+ Ghi nhận</a></x-page-header>
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
<form method="GET" class="mb-6 grid gap-3 md:grid-cols-3 lg:grid-cols-6"><input name="month" type="number" min="1" max="12" value="{{ request('month') }}" placeholder="Tháng" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><input name="year" type="number" min="2000" max="2100" value="{{ request('year') }}" placeholder="Năm" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><select name="department_id" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Tất cả phòng ban</option>@foreach($departments as $department)<option value="{{ $department->id }}" @selected(request('department_id') == $department->id)>{{ $department->name }}</option>@endforeach</select><select name="status" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Tất cả trạng thái</option>@foreach(['present'=>'Có mặt','late'=>'Đi muộn','absent'=>'Vắng','leave'=>'Nghỉ phép'] as $value=>$label)<option value="{{ $value }}" @selected(request('status') === $value)>{{ $label }}</option>@endforeach</select><input name="search" value="{{ request('search') }}" placeholder="Mã/tên/email nhân viên" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Lọc</button><a href="{{ route('hr.attendances.index') }}" class="rounded-lg px-4 py-2 text-center text-sm font-semibold text-slate-500">Đặt lại</a></form>
<div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Ngày</th><th class="px-3 py-3">Nhân viên</th><th class="px-3 py-3">Phòng ban</th><th class="px-3 py-3">Giờ</th><th class="px-3 py-3">Trạng thái</th><th class="px-3 py-3 text-right">Thao tác</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attendances as $attendance)<tr><td class="px-3 py-4">{{ $attendance->work_date->format('d/m/Y') }}</td><td class="px-3 py-4"><span class="font-semibold">{{ $attendance->employee->user->name }}</span><br><span class="text-xs text-slate-500">{{ $attendance->employee->employee_code }}</span></td><td class="px-3 py-4">{{ $attendance->employee->department->name }}</td><td class="px-3 py-4">{{ $attendance->check_in ?: '—' }} - {{ $attendance->check_out ?: '—' }}</td><td class="px-3 py-4"><x-status-badge :status="$attendance->status" :label="ucfirst($attendance->status)" /></td><td class="px-3 py-4 text-right"><a href="{{ route('hr.attendances.edit', $attendance) }}" class="font-semibold text-indigo-600">Sửa</a></td></tr>@empty<tr><td colspan="6" class="px-3 py-5"><x-empty-state title="Chưa có bản ghi chấm công" description="Hãy điều chỉnh bộ lọc hoặc ghi nhận bản ghi mới." /></td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $attendances->links() }}</div></div>
@endsection
