@extends('layouts.app')
@section('title', 'Chi tiết đơn nghỉ')
@section('content')
<x-page-header eyebrow="Cá nhân" title="Chi tiết đơn nghỉ" description="Thông tin và trạng thái xử lý đơn xin nghỉ."><a href="{{ route('employee.leave-requests.index') }}" class="app-button-secondary">Quay lại</a></x-page-header>
<div class="app-panel max-w-3xl p-5 sm:p-6"><dl class="grid gap-5 sm:grid-cols-2"><div><dt class="app-label">Loại nghỉ</dt><dd class="mt-1">{{ ['annual'=>'Nghỉ phép năm','sick'=>'Nghỉ ốm','unpaid'=>'Nghỉ không lương','other'=>'Khác'][$leaveRequest->leave_type] ?? $leaveRequest->leave_type }}</dd></div><div><dt class="app-label">Trạng thái</dt><dd class="mt-1"><x-status-badge :status="$leaveRequest->status" :label="['pending'=>'Đang chờ','approved'=>'Đã duyệt','rejected'=>'Từ chối','cancelled'=>'Đã hủy'][$leaveRequest->status] ?? $leaveRequest->status" /></dd></div><div><dt class="app-label">Thời gian</dt><dd class="mt-1">{{ $leaveRequest->start_date->format('d/m/Y') }} - {{ $leaveRequest->end_date->format('d/m/Y') }}</dd></div><div><dt class="app-label">Ngày gửi</dt><dd class="mt-1">{{ $leaveRequest->created_at->format('d/m/Y H:i') }}</dd></div><div class="sm:col-span-2"><dt class="app-label">Lý do</dt><dd class="mt-1 whitespace-pre-line">{{ $leaveRequest->reason }}</dd></div></dl>@if($leaveRequest->status === 'pending')<form method="POST" action="{{ route('employee.leave-requests.cancel', $leaveRequest) }}" class="mt-6">@csrf @method('PATCH')<button type="submit" class="rounded-xl border border-red-200 px-4 py-2.5 text-sm font-semibold text-red-600 hover:bg-red-50" onclick="return confirm('Hủy đơn xin nghỉ này?')">Hủy đơn</button></form>@endif</div>
@endsection
