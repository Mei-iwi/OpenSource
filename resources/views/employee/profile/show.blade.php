@extends('layouts.app')
@section('title', 'Hồ sơ cá nhân')
@section('content')
<x-page-header eyebrow="Cá nhân" title="Hồ sơ của tôi" description="Thông tin hồ sơ và công việc của tài khoản đang đăng nhập.">@if($employee)<a href="{{ route('employee.profile.edit') }}" class="app-button-primary">Chỉnh sửa hồ sơ</a>@endif</x-page-header>
<div class="app-panel p-5 sm:p-6">@if($employee)<div class="mb-6 flex flex-col gap-4 border-b border-[var(--app-border)] pb-6 sm:flex-row sm:items-center">@if($employee->avatar_path)<img src="{{ Storage::disk('public')->url($employee->avatar_path) }}" alt="Ảnh đại diện {{ $employee->user->name }}" class="h-24 w-24 rounded-2xl object-cover shadow-sm">@else<div class="flex h-24 w-24 items-center justify-center rounded-2xl bg-orange-100 text-3xl font-bold text-orange-700">{{ strtoupper(substr($employee->user->name, 0, 1)) }}</div>@endif<div><h2 class="text-xl font-bold text-[var(--app-text)]">{{ $employee->user->name }}</h2><p class="mt-1 text-sm text-[var(--app-muted)]">{{ $employee->employee_code }}</p><div class="mt-3"><x-status-badge :status="$employee->employment_status" :label="$employee->employment_status === 'active' ? 'Đang làm việc' : 'Ngừng làm việc'" /></div></div></div><dl class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><div><dt class="app-label">Email</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->user->email }}</dd></div><div><dt class="app-label">Phòng ban</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->department?->name ?? '—' }}</dd></div><div><dt class="app-label">Chức vụ</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->position ?: '—' }}</dd></div><div><dt class="app-label">Ngày vào làm</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->hire_date?->format('d/m/Y') ?: '—' }}</dd></div><div><dt class="app-label">Điện thoại</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->phone ?: '—' }}</dd></div><div><dt class="app-label">Ngày sinh</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->date_of_birth?->format('d/m/Y') ?: '—' }}</dd></div><div class="sm:col-span-2 lg:col-span-3"><dt class="app-label">Địa chỉ</dt><dd class="mt-1 text-sm text-[var(--app-text)]">{{ $employee->address ?: '—' }}</dd></div></dl>@else<x-empty-state title="Chưa có hồ sơ nhân viên" description="Tài khoản chưa được cấu hình hồ sơ. Vui lòng liên hệ Admin/HR." />@endif</div>
@endsection
