@extends('layouts.app')
@section('title', 'Lịch sử chấm công')
@section('content')
<x-page-header eyebrow="Employee / Cá nhân" title="Lịch sử chấm công" description="Chỉ hiển thị lịch sử của tài khoản đang đăng nhập." />
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><form method="GET" class="mb-6 flex flex-wrap gap-3"><input name="month" type="number" min="1" max="12" value="{{ request('month') }}" placeholder="Tháng" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><input name="year" type="number" min="2000" max="2100" value="{{ request('year') }}" placeholder="Năm" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Lọc</button><a href="{{ route('employee.attendances.index') }}" class="rounded-lg px-4 py-2 text-sm font-semibold text-slate-500">Đặt lại</a></form><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-500"><tr><th class="px-3 py-3">Ngày</th><th class="px-3 py-3">Check-in</th><th class="px-3 py-3">Check-out</th><th class="px-3 py-3">Trạng thái</th><th class="px-3 py-3">Ghi chú</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attendances as $attendance)<tr><td class="px-3 py-4">{{ $attendance->work_date->format('d/m/Y') }}</td><td class="px-3 py-4">{{ $attendance->check_in ?: '—' }}</td><td class="px-3 py-4">{{ $attendance->check_out ?: '—' }}</td><td class="px-3 py-4"><x-status-badge :status="$attendance->status" :label="ucfirst($attendance->status)" /></td><td class="px-3 py-4">{{ $attendance->note ?: '—' }}</td></tr>@empty<tr><td colspan="5" class="px-3 py-5"><x-empty-state title="Chưa có lịch sử chấm công" /></td></tr>@endforelse</tbody></table></div>@if($employee)<div class="mt-6">{{ $attendances->links() }}</div>@endif</div>
@endsection
